# ── Secrets — ห้าม commit เด็ดขาด (repo เป็น public) ──
.streamlit/secrets.toml
API_flashalpha.txt
*.key

# ── Python ──
__pycache__/
*.pyc
.venv/
venv/

# ── Claude Code ──
.claude/

# report preview (generated by send_report.py --dry-run)
preview.html
.gate-state.json
.mail-sent.json

# รายงาน 0DTE ที่ generate เอง — ข้อมูลดิบ (zero_dte/**.csv) commit · รายงานไม่ต้อง
zero_dte/**/report-*.html
