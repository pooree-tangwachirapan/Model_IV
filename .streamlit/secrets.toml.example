# คัดลอกไฟล์นี้เป็น secrets.toml แล้วใส่ key จริง (secrets.toml อยู่ใน .gitignore — ห้าม commit)
# บน Streamlit Cloud: App → Settings → Secrets แล้ววางบรรทัดเดียวกันนี้
FLASHALPHA_API_KEY = "YOUR_FLASHALPHA_API_KEY_HERE"
